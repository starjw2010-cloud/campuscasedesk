# Local MariaDB via docker-compose.yml.
# Copy values into your shell or a local .env file. Do not commit real secrets.

DATA_BACKEND=mariadb
MARIADB_URL=mysql://campusflow:campusflow_dev@127.0.0.1:3307/campusflow
IMPORT_MARIADB_ON_START=false

HOST=127.0.0.1
PORT=8771
MCP_AUTH_MODE=no_auth
MCP_SHARED_SECRET=

